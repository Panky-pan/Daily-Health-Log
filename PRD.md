# PRD ·「每日健康打卡」(Daily-Health-Log)

> 版本：v1.1　撰写日期：2026-09-21
> 依据文档：research.md（2026-09-19 竞品调研 + MVP 方案）
> 读者：零基础开发者（Panky）本人，以及未来任何想读懂这个项目的人
>
> **修订记录**
>
> | 版本 | 日期 | 改动 |
> |---|---|---|
> | v1.0 | 2026-09-21 | 初稿：按 research.md 展开八大节 |
> | v1.1 | 2026-09-21 | 目标用户改为「先单人后开放」（邮箱登录列二期）；自检后补定 streak/坚持率统计口径、提醒条与部分填写的交互 |

---

## 一、产品概述

### 1.1 一句话定位

**一个只服务自己的极简健康打卡网页**：每天花 1 分钟记录运动、饮食、体重和饮水，坚持情况一屏看清，目标没达成时页面内提醒。

### 1.2 产品说明

| 项目 | 内容 |
|---|---|
| 服务对象 | 第一用户是 Panky 本人；产品从设计之初为「未来开放给更多人」预留演进空间，但本期（MVP）只服务本人 |
| 产品形态 | 纯前端网页（HTML + CSS + JavaScript），双击/浏览器打开即用 |
| 数据存储 | 浏览器 localStorage，数据归自己，不上传任何服务器 |
| 账号体系 | 无。不注册、不登录、不区分用户 |
| 商业化 | 无。无广告、无会员、无付费点 |
| 核心闭环 | 每日记录 → 打卡日历与坚持率 → 体重趋势 → 目标提醒 |

### 1.3 设计原则（贯穿所有功能）

1. **记录成本压到最低**：打卡一次的目标耗时 ≤ 1 分钟。凡是需要查数据库、算热量的功能一律不做。
2. **数据可见、可坚持**：做了记录就要能看到结果——日历、坚持率、streak、趋势图，让"坚持"本身被看见。
3. **失败要体面**：localStorage 可能被清空、某天可能漏打卡，任何情况下页面不能白屏或假死，缺什么就提示什么。

---

## 二、目标用户

### 2.1 本期（MVP）用户

| 维度 | 描述 |
|---|---|
| 唯一用户 | Panky 本人（第一用户） |
| 使用场景 | 每天固定时间（如晚上）打开网页，填一条当日记录；偶尔翻翻日历和趋势图回顾 |
| 用户画像 | 想建立运动 + 饮食自觉的健康习惯、被大平台 App 的复杂度和广告劝退的轻度用户 |
| 核心诉求 | ① 记得快（1 分钟搞定）② 看得见坚持（连续天数、漏卡一眼看出）③ 数据是自己的（本地、无账号、无追踪） |
| 不满足的诉求 | 计算精确热量、拍照识餐、社交分享、设备同步——这些明确不承诺（见"后续功能"与"明确不做"） |

### 2.2 产品定位与演进方向（2026-09-21 拍板）

- **初心**：这个网站源于 Panky 自己的真实需求，先用自己当第一个用户，把「记录 + 坚持 + 趋势 + 目标提醒」的核心闭环打磨好用。
- **愿景**：核心闭环验证稳定后（二期启动条件：MVP 上线且本人连续使用满 1 个月），升级为开放产品——新用户通过**简单邮箱登录**即可创建自己的账号，拥有独立的健康记录。
- **演进原则**：先做通单机版，再叠账号体系。二期引入邮箱登录时，数据必须同步上云（否则换设备登录毫无意义），届时需一并规划账号安全（密码存储、找回密码）与用户数据隔离。MVP 阶段不做任何账号相关设计，但记录数据结构保持清晰、可导出，为将来迁移留余地。

---

## 三、MVP 功能（本期必做）

> 每个功能都写明「用户动作」和「可观察结果」。可观察结果 = 做完动作后，肉眼能确认发生了什么。

### F1 目标设置（首次打开必经）

- **用户动作**：第一次打开网页时，在设置区填入两个目标值——「每日运动分钟数」和「每日饮水量」，点击保存。之后可随时进入设置修改。
- **可观察结果**：
  - 首次打开时，在完成设置前，今日打卡页不显示正常内容，而是先出现设置表单（或者顶部有醒目的"先设置目标"提示）。
  - 保存后：设置值显示在页面上（如"今日目标：运动 30 分钟 / 饮水 2000ml"），刷新网页后目标值仍在。
  - 修改目标后：页面上所有引用目标的地方（提醒条、对比文案）随之更新。

### F2 今日打卡（核心功能）

- **用户动作**：在今日打卡页填写一张表单：
  1. 运动：选类型（下拉/按钮，覆盖 5~6 种常见运动，如散步、慢跑、跳绳、骑行、力量训练、瑜伽）+ 填时长（分钟）；
  2. 三餐：早餐 / 午餐 / 晚餐各写一句「吃了什么」，并各选一个标签：清爽 / 标准 / 丰盛；
  3. 体重：填当日体重（kg，允许一位小数）；
  4. 饮水：填当日饮水量（ml）；
  5. 点击「保存今日打卡」。
- **可观察结果**：
  - 保存后：运动卡路里按内置换算表**自动估算并显示**出来（如"慢跑 30 分钟 ≈ 300 大卡"），用户不需要也不会手动填热量数字。
  - 保存后：表单显示当天已打卡的状态（如绿色"今日已打卡 ✓"），同一天可以再次打开编辑并覆盖保存，不会产生两条记录。
  - 刷新网页后：当天的打卡内容仍然显示，数据没丢。
  - 如果某项留空：允许部分填写（见异常状态 E3）。

### F3 打卡日历与坚持率（历史记录页）

- **用户动作**：切到「历史记录」页查看。
- **可观察结果**：
  - 出现一张当月日历：打了卡的日子有标记（如绿色圆点/色块），没打卡的日子是空白，今天有特殊标记。
  - 显示 streak：**当前连续打卡天数**和**历史最长连续纪录**。统计口径（开发必须照此实现）：
    - 当前 streak：若今天已打卡，从今天往前数连续有记录的天数；若今天还没打卡，**从昨天往前数**（不因"今天还没打"清零，今天打卡后自动 +1）；
    - 历史最长：全部记录中曾出现过的最大连续天数。
  - 显示该月的坚持率：已打卡天数 ÷ 应打卡天数，以百分比呈现。统计口径：分母 = startDate 起**至昨天**的天数（当天未结束不计入，避免"今天还没打卡就被扣坚持率"）；分子 = 其中实际打卡的天数。
  - 点击日历上某个已打卡的日子，可以看到那天的记录内容（运动、三餐、体重、饮水）。

### F4 趋势图表（趋势页）

- **用户动作**：切到「趋势」页查看。
- **可观察结果**：
  - 出现一张体重折线图：横轴为日期，纵轴为体重 kg，按有记录的日期连成线。
  - 出现一张每周运动时长柱状图：横轴为周（近 4~8 周），纵轴为该周运动分钟合计。
  - 数据不足时的表现见异常状态 E4，不显示空图或报错。

### F5 目标提醒条（页内提醒）

- **用户动作**：无主动动作。打开今日打卡页时自动触发。
- **可观察结果**：
  - 当天还没打卡，或已打卡但未达目标（运动时长 < 目标，或饮水 < 目标）时：页面顶部出现一条黄色提醒条，写清差多少（如"今天运动还差 10 分钟，加油！"）。
  - 已达标时：提醒条变为绿色的达标状态（如"今日目标已达成 ✓"）。
  - 提醒只出现在页面内，不弹窗、不发通知（本期无推送能力）。
  - 与部分填写（E3）的交互：某项目标对应的数据当天没填时，按 0 计算、显示为未完成并提示差值（如运动填了、饮水没填 → 显示"饮水还差 xxx ml"），不因没填而跳过该项提醒。

---

## 四、后续功能（本期不做，仅规划）

| 功能 | 说明 | 暂不做的理由 |
|---|---|---|
| **邮箱登录 + 云端数据同步（二期第一优先级）** | 新用户邮箱注册/登录即创建账号，打卡数据同步上云，换设备不丢 | 需要引入后端（账号系统 + 云数据库），是把单机工具升级为在线服务的大改动。启动条件：MVP 上线且本人连续使用满 1 个月。届时需同步规划密码安全、找回密码、用户数据隔离 |
| 数据导出/备份 | 一键导出 JSON/CSV | MVP 先跑通核心闭环；导出同时是二期数据迁移的基础，建议与账号功能同期或更早 |
| 饮食拍照上传 | 打卡时可附餐食照片 | 照片转 base64 存 localStorage 体积过大（压缩后一张几百 KB，配额约 5MB），存两周即爆。等有云存储或 IndexedDB 方案再上 |
| 更多运动类型与自定义换算表 | 用户可增删运动类型、自定义卡路里换算 | 先用内置 5~6 种验证需求 |
| 饮食热量记录 | 记录每餐精确热量 | 已拍板（2026-09-19）：只打标签不算热量，除非未来改变记录习惯 |
| 周报/月报复盘 | 自动生成每周小结（坚持率、体重变化、运动量对比） | 依赖数据积累，先看图表够用 |
| 多设备同步/云端存储 | 换设备数据不丢 | 需要服务器或云服务，与"纯本地、零成本"原则冲突，谨慎评估后再说 |
| 奖牌、游戏化挑战 | 连续打卡成就徽章等 | 锦上添花，等核心闭环稳定后考虑 |
| 设备同步（手环/Apple Health 等） | 自动导入运动、体重数据 | 依赖第三方授权，成本远超个人项目预算 |

### 明确不做（长期边界，非"以后做"）

食物数据库 / 拍照识餐、社区与分享、智能训练计划、饮食推荐、提醒推送（消息通知）、会员付费、广告。理由见 research.md 第二节。

> 说明：账号注册/登录在 research.md 中曾列为"明确不做"，2026-09-21 起调整为**二期规划**（见 2.2 演进方向与后续功能表），不再属于长期边界。MVP 阶段仍然完全无账号。

---

## 五、页面与交互要求

### 5.1 页面结构（3 页 + 1 设置区）

| 页面 | 路径/入口 | 主要内容 |
|---|---|---|
| ① 今日打卡页 | 默认首页 | streak 大字区 → 目标提醒条 → 打卡表单（F2） |
| ② 历史记录页 | 顶部/侧边导航切换 | 打卡日历 + 坚持率 + streak 统计 + 单日详情（F3） |
| ③ 趋势页 | 顶部/侧边导航切换 | 体重折线图 + 周运动柱状图（F4） |
| 设置区 | 首次打开强制出现；之后从页面入口进入 | 目标值设置与修改（F1） |

### 5.2 交互要求

1. **页面切换**：三个页面用导航切换（单页应用式或三个 HTML 文件均可，由实现时定，但切换后数据共享、刷新不丢状态）。
2. **表单**：字段清晰标注单位（分钟 / kg / ml）；标签用三个可点选的按钮而不是手输；保存按钮点击后有明确反馈（状态文字变化即可，不要求弹窗）。
3. **修改已打卡记录**：当天重复保存 = 覆盖更新，页面上能看到旧值被新值替换。
4. **风格**：奶油色底 + 绿色主色的清新配色、圆角卡片、轻量动效（如打卡成功的状态变化）。要求视觉统一，不堆砌动画。
5. **响应式**：以桌面浏览器为主，手机浏览器打开时表单和日历不错位（基础自适应即可，不要求完美适配）。

---

## 六、数据字段

> 存储位置：浏览器 localStorage。具体存一个 JSON 对象还是分多个 key，由实现时决定，但字段含义如下。

### 6.1 单日打卡记录（每天最多一条，以日期为唯一标识）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| date | 日期（如 2026-09-21） | 是 | 当天日期，作为记录的唯一 key |
| exerciseType | 文本（枚举） | 否 | 运动类型：散步/慢跑/跳绳/骑行/力量训练/瑜伽（清单可随实现微调） |
| exerciseMinutes | 数字 | 否 | 运动时长（分钟），0 或正整数 |
| exerciseCalories | 数字（自动） | 自动 | 由换算表根据类型 × 时长自动估算，用户不可编辑 |
| mealBreakfastText | 文本 | 否 | 早餐吃了什么（一句话） |
| mealBreakfastTag | 枚举 | 否 | 清爽 / 标准 / 丰盛 |
| mealLunchText | 文本 | 否 | 午餐吃了什么 |
| mealLunchTag | 枚举 | 否 | 清爽 / 标准 / 丰盛 |
| mealDinnerText | 文本 | 否 | 晚餐吃了什么 |
| mealDinnerTag | 枚举 | 否 | 清爽 / 标准 / 丰盛 |
| weightKg | 数字 | 否 | 当日体重（kg，允许一位小数，合理范围校验见 E5） |
| waterMl | 数字 | 否 | 当日饮水量（ml，正整数） |

### 6.2 全局设置（存一份）

| 字段 | 类型 | 说明 |
|---|---|---|
| goalExerciseMinutes | 数字 | 每日运动目标（分钟） |
| goalWaterMl | 数字 | 每日饮水目标（ml） |
| startDate | 日期 | 首次打卡/开始使用的日期，用于计算坚持率分母 |

### 6.3 运动卡路里内置换算表（写死在代码里，不做配置）

| 运动类型 | 估算值（供参考，实现时可微调） |
|---|---|
| 散步 | ≈ 4 大卡/分钟 |
| 慢跑 | ≈ 10 大卡/分钟 |
| 跳绳 | ≈ 12 大卡/分钟 |
| 骑行 | ≈ 8 大卡/分钟 |
| 力量训练 | ≈ 6 大卡/分钟 |
| 瑜伽 | ≈ 3 大卡/分钟 |

---

## 七、异常状态

> 原则：任何异常下页面不能白屏、不能静默丢数据。

| 编号 | 场景 | 预期表现 |
|---|---|---|
| E1 | 首次使用，localStorage 无任何数据 | 不报错；引导进入目标设置（F1）；日历/趋势页显示"还没有记录"类的空状态文案 |
| E2 | localStorage 数据被浏览器清空（清缓存、隐私模式等） | 页面当首次使用处理（回到 E1），不白屏不报错；可在设置区或空状态中提示"数据仅存于本浏览器" |
| E3 | 表单部分字段留空 | 允许保存：只存填了的项目，没填的记为空；但至少要有 1 项内容才允许保存，全空时保存按钮给出提示（如"至少填写一项"） |
| E4 | 趋势页数据不足（体重记录 < 2 个点，或运动记录为 0） | 对应图表区域显示"记录太少，先去打卡几次吧"的引导文案，不渲染空图、不显示 NaN |
| E5 | 输入了不合理的数值（体重 0 或 500kg、运动时长 -5、饮水为负等） | 保存前拦截：在对应字段旁显示红字提示，不保存该条记录 |
| E6 | 同一天重复打卡 | 不新建记录，覆盖保存当天的旧记录，页面状态文字提示"已更新今日记录" |
| E7 | localStorage 写入失败（配额满、浏览器限制等） | 保存按钮反馈"保存失败，请检查浏览器存储设置"，不假装成功 |
| E8 | 跨月/跨年查看日历 | 日历可切换上/下月；未来日期不可打卡（无打卡入口或点后提示"还没到那天"） |

---

## 八、验收标准（MVP 完成的定义）

> 以下每条都必须能亲手操作验证，全部通过才算 MVP 完成。

### 8.1 功能验收

| 编号 | 验收项 | 操作方法 | 通过标准 |
|---|---|---|---|
| A1 | 首次打开引导 | 清空 localStorage 后打开网页 | 出现目标设置，未设置时主流程无法跳过 |
| A2 | 目标设置持久化 | 设置目标 → 刷新网页 | 目标值仍正确显示 |
| A3 | 打卡保存 | 填写运动 + 三餐 + 体重 + 饮水 → 保存 | 显示卡路里自动估算结果和"今日已打卡"状态 |
| A4 | 数据持久化 | 打卡后关闭网页再重新打开 | 今日打卡页仍显示当天记录 |
| A5 | 覆盖打卡 | 同一天修改某项后再次保存 | 显示"已更新"，旧值被替换，历史页单日详情同步为新值 |
| A6 | 日历与坚持率 | 连续打卡 3 天后打开历史页 | 日历 3 天有标记，坚持率和 streak 数字与实际一致（手动数一遍核对） |
| A7 | 单日详情 | 点击日历中已打卡的日子 | 显示当天完整记录内容 |
| A8 | 趋势图 | 打卡 ≥ 3 天不同体重后打开趋势页 | 体重折线图正确反映数值走势；周运动柱状图数字与记录合计一致 |
| A9 | 目标提醒条 | 当天未达标 / 已达标两种状态下分别查看 | 黄色提示（写明差值）/ 绿色达标，文案正确 |
| A10 | 卡路里换算 | 分别记录两种不同运动各 30 分钟 | 估算卡路里与换算表一致 |

### 8.2 异常验收

| 编号 | 验收项 | 操作方法 | 通过标准 |
|---|---|---|---|
| B1 | 空数据 | 清空存储打开页面 | 无报错，回 E1 表现 |
| B2 | 全空保存 | 不填任何内容点保存 | 被拦截并有提示 |
| B3 | 非法输入 | 输入体重 500、时长 -5 | 被拦截，有红字提示 |
| B4 | 浏览器兼容 | Chrome 与 Edge 各打开一次 | 页面正常、数据各自独立保存 |

### 8.3 体验验收

| 编号 | 验收项 | 通过标准 |
|---|---|---|
| C1 | 记录耗时 | 熟练后完成一次完整打卡 ≤ 1 分钟 |
| C2 | 视觉 | 三页配色、圆角卡片风格统一；奶油色 + 绿色清新配色落地 |
| C3 | 手机可用 | 手机浏览器打开，表单可填写、日历不错位 |

---

## 附：与 research.md 的对应关系

- MVP 3 页 + 设置区、6 项数据、饮食只打标签、卡路里自动估算、页面内提醒、streak——均来自 research.md 第二节，本 PRD 将其展开为可验收的功能条目。
- 后续功能与"明确不做"边界基本与 research.md 一致；**唯一调整**：账号体系（邮箱登录 + 云端同步）由"明确不做"改为二期规划（2026-09-21 Panky 拍板，见 2.2），MVP 范围未变。
- 本 PRD 不含任何代码与技术实现细节，技术选型（单页 vs 多文件、图表库等）留给开发阶段决定。
